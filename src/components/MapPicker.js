import React from 'react';

const MapPicker = (props) => {
        return (
            <React.Fragment>
                <p>Map picker</p>
            </React.Fragment>        
        )    
}

export default MapPicker;